import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    title: {
        textAlign: 'center',
    },
    addActivityBtn: {
        textAlign: 'end',
    },
}))

export default useStyles;